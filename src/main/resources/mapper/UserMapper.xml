<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.project.mapper.UserMapper">
    <insert id="addUser" keyProperty="uid">
        insert into t_user (name) values (#{name})
    </insert>
</mapper>









